import {PlayerList} from "@/entities/player/ui/PlayerList";
import {useState} from "react";
import Card from "react-bootstrap/Card";
import Collapse from "react-bootstrap/Collapse";

export const DeviceList = () => {

  return (
    <div>
      {devices.map((device) => (
        
      ))}
    </div>
  );
};
